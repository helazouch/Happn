// import express,{ Router } from 'express';
// import eventRoutes from './events';

// const router = Router();
// router.use(express.json());
// router.use('/events', eventRoutes);
// // ...autres routes (auth, participation etc.)

// export default router;